<nav class="navbar">
  <div class="navbar__logo">
    <a href="/"><img src="/style/images/retro.png" alt="Retro Logo" /></a>
  </div>

  <button class="navbar__toggle" aria-label="Toggle menu" onclick="toggleMenu()">☰</button>

  <ul class="navbar__links" id="navbarLinks">
    <li>
      <a href="/challenges" class="{{#if (eq currentPage 'challenges')}}active{{/if}}">Challenges</a>
    </li>
    <li><a href="/yearly-challenge-2025" class="{{#if (eq currentPage 'yearly-challenge')}}active{{/if}}">52 GAMES</a></li>
    <li><a href="/kaizo-hacks" class="{{#if (eq currentPage 'kaizo-hacks')}}active{{/if}}">KAIZO</a></li>
    <li>
      <a href="/backlog" class="{{#if (eq currentPage 'backlog')}}active{{/if}}">Backlog</a>
    </li>
    <li>
      <a href="/about" class="{{#if (eq currentPage 'about')}}active{{/if}}">ABOUT</a>
    </li>
  </ul>
</nav>

<script>
window.addEventListener('scroll', () => {
  const nav = document.querySelector('.navbar');
  if (window.scrollY > 10) nav.classList.add('scrolled');
  else nav.classList.remove('scrolled');
});

  // mobile menu toggle for the hamburger
  window.toggleMenu = function () {
    const panel = document.getElementById('navbarLinks');
    const btn = document.querySelector('.navbar__toggle');
    const open = panel.classList.toggle('is-open');
    if (btn) btn.setAttribute('aria-expanded', open ? 'true' : 'false');
  };
</script>